import type { TopicMeta } from '@prep/core'

export const meta: TopicMeta = {
  slug: 'strings',
  title: 'Strings, code units and code points',
  summary:
    'Why an emoji has a length of 2, what a tagged template receives, and when two identical strings are not equal.',
  order: 30,
  tags: ['strings', 'unicode', 'templates'],
  prerequisites: ['javascript/types-and-coercion'],
}
