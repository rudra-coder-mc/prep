import 'server-only'

/**
 * Server-side entry point for content. The loader itself lives in ./loader so
 * that build scripts and tests can use it outside a React Server Component.
 */
export * from './loader'
