import type { TopicMeta } from '@/content/schema'

export const meta: TopicMeta = {
  slug: 'scope-and-hoisting',
  title: 'Scope, hoisting and the temporal dead zone',
  summary: 'Where a name can be seen from, and what happens when you read it too early.',
  order: 20,
  difficulty: 'medium',
  tags: ['scope', 'hoisting', 'declarations'],
  prerequisites: [],
}
