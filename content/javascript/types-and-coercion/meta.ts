import type { TopicMeta } from '@/content/schema'

export const meta: TopicMeta = {
  slug: 'types-and-coercion',
  title: 'Types and coercion',
  summary: 'The seven primitives, what `==` really does, and why `typeof null` is "object".',
  order: 10,
  difficulty: 'medium',
  tags: ['types', 'coercion', 'equality'],
  prerequisites: [],
}
